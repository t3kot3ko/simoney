.jumbotron
  %h1 Simoney
  .row
    .col-md-8
      %p Description of this application
    .col-md-4
      = form_tag(user_session_path, method: :post, class: "form-horizontal") do |f|
        .form-group
          %label.control-label メールアドレス
          = text_field :user, :email, class: 'form-control', required: true
          // = error_span(@user[:name])
        .form-group
          %label.control-label パスワード
          = password_field :user, :password, class: 'form-control', required: true
          // = error_span(@user[:name])
        .form-group
          = submit_tag "ログイン", class: "btn btn-primary"
          %a.btn.btn-primary 新規登録


%h2 calendar sample

#calendar

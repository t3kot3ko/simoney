= form_for @property_fix_history do |f|
  - if @property_fix_history.errors.any?
    #error_explanation
      %h2= "#{pluralize(@property_fix_history.errors.count, "error")} prohibited this property_fix_history from being saved:"
      %ul
        - @property_fix_history.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
